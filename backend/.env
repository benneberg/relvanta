# MongoDB Configuration
MONGO_URL="mongodb://localhost:27017"
DB_NAME="test_database"

# CORS Origins (comma-separated)
CORS_ORIGINS="*"

# Firebase Configuration (Backend)
# Option 1: Provide path to service account JSON file
# FIREBASE_CREDENTIALS_PATH=secrets/firebase-admin.json

# Option 2: Provide credentials as JSON string (for production/serverless)
# FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project",...}

# For development, you can leave these commented out
# Authentication will show warnings but won't crash the server
# Add real credentials when ready to test authentication