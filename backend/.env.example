# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017
DB_NAME=test_database

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3001,https://your-domain.com

# Firebase Configuration (Backend)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Then set the path to the JSON file or provide as JSON string

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CREDENTIALS_PATH=secrets/firebase-admin.json

# Or provide credentials as JSON string (for serverless deployments)
# FIREBASE_CREDENTIALS_JSON='{"type":"service_account",...}'
